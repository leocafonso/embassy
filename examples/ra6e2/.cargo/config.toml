[target.'cfg(all(target_arch = "arm", target_os = "none"))']
runner = "probe-rs run --chip R7FA6E2BB"
rustflags = ["-C", "link-arg=--nmagic", "-C", "link-arg=-Tlink.x", "-C", "link-arg=-Tdefmt.x"]

[build]
target = "thumbv8m.main-none-eabihf"

[env]
DEFMT_LOG = "info"
